<%= form_for :results, url:results_path do |f| %>
<%= f.text_field :weight  %>
<%= select_tag(:exercise, options_for_select(Exercise.all.pluck(:name))) %>
<%= select_tag(:food, options_for_select(Food.all.pluck(:name))) %>
<%= f.submit "Calculate" %> 
<% end %>
